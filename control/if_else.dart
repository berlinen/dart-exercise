// if 和 else

// Dart 支持 if - else 语句，其中 else 是可选的，

// 和 JavaScript 不同， Dart 的判断条件必须是布尔值，不能是其他类型。

void demo () {
  // if (isRaining()) {
  //   you.bringRainCoat();
  // } else if (isSnowing()) {
  //   you.wearJacket();
  // } else {
  //   car.putTopDown();
  // }
}